[project]
name = "ladym-client"
version = "0.1.0"
description = "Python SDK for the LadyM HTTP data-plane (`ladym serve --http`), zero runtime dependencies"
readme = "README.md"
authors = [
    { name = "yuhaochen", email = "cyh278889789@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.9,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
